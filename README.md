GenMesh is a standalone C++ library for computational mesh management. 
It provides high-performance, extensible data structures to represent mesh entities and 
their connectivities, serving as a reusable foundation for finite element, finite volume, 
and other scientific computing frameworks. 

It can be integrated into external projects as a generic mesh infrastructure.
